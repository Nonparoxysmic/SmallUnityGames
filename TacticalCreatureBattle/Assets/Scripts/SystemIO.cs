using System.IO;
using UnityEngine;

public static class SystemIO
{
    //static string _creatureDataFolderPath = Application.persistentDataPath + "/creature";
    //static string _worldDataFolderPath = Application.persistentDataPath + "/world";

    // TODO: Read data from and write data to files, and make that data
    // available to components like WorldLoader and CreatureData.
}
